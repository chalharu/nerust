These tests test the LDM^ glitch. When LDM to user mode registers, the next register access uses the OR'd value of the user mode and banked register if it exists.

These tests are hardware dependent, and may not work on all models, I have personally tested on GBP.
