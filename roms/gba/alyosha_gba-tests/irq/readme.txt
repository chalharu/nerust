Tests for reading / writing IF and IE around time interrupts would occur.

Also tests disabling timer around time interrrupt would trigger (timer runs one cycle after disabling, and interrupt should still occur in this case.)
