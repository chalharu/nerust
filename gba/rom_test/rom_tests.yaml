rom_root: ../../roms/gba
expected_failures:
# burst_into_tears: GRADUATED 3/3. force_nseq: GRADUATED 88/88.
# cancel_ie: rows 2/4 fail on 1-tick race boundaries; do not refit.
- nba_archive_irq_cancel_ie
# irq_delay: GRADUATED 3/3 (write-asserted line, +1).
# cancel_ime: GRADUATED 8/8.
# Affine refs fail on reference state, not emulation.
- PeterLemon_BGRotZoomMode2
- nba_archive_ppu_exact_timing
# Photo refs fail on dimensions first (4000x3000 vs 240x160).
- nba_archive_ppu_mode2
- nba_archive_ppu_mode3
- nba_archive_ppu_mode4
- nba_haltcnt
- nba_ppu_bgpd
- nba_ppu_bgx
- nba_ppu_dispcnt_latch
- nba_ppu_greenswap
# status_irq_dma / ram_access_timing are interactive; no headless hooks.
- nba_ppu_ram_access_timing
- nba_ppu_sprite_hmosaic
- nba_ppu_status_irq_dma
- PeterLemon_FastLine
- PeterLemon_FastLineClip
# PeterLemon_Timers: inconsistent ref; never fit timers to it.
- PeterLemon_Timers
# Video class: refs are unmapped-moment screenshots.
- PeterLemon_GRB12LZDIFFRLEVideo
- PeterLemon_Rickroll
- PeterLemon_GRB15LZVideo
- PeterLemon_GRB12LZVideo
- PeterLemon_GRB12LZIPSVideo
- PeterLemon_DevoTimeOutForFun
- PeterLemon_GRB15LZIPSVideo
- PeterLemon_TouhouBadApple
- PeterLemon_BigBuckBunny
- PeterLemon_3DEngine
- PeterLemon_CylinderMap
# Velocity/Acceleration: interacted refs.
- PeterLemon_Velocity
- PeterLemon_Acceleration
# mgba_suite_timing: GRADUATED 2026-09-18 (2020/2020 subchecks pass;
# DMA GamePak fill-collision arbitration models the last P-ON ROM-DMA
# residual; matrix 158/94/0, lib 357 green).
# mgba_suite_timers: 283/936 subchecks fail (prescaler-0 dominated;
# 1xs all -1, 16xs mostly -16, values small phase deltas). Traced
# "0b, 0x0005" 1d1i tight: the 2nd poll sample races IRQ#1 entry by ~1
# cycle (sample would-be +11 vs entry +10 from enable; S1 +3, loop 8,
# overflow +7 all GBATEK/nba-exact). Global shifts are all pinned:
# nba reload 7/7 (word-enable phase), start-stop 3/8 (start/stop),
# irq-delay 92/112/120 (entry sum), Timing 2020 (CPU takes). Grids
# tried and reverted: word-latch +1 (timers -9, reload breaks),
# no-load-erase (matrix breaks incl Timing), line/prologue split
# (irq-delay 0/3), execute-first-on-tie variants (assert-vacuous:
# timers -18, irq-delay breaks; retire-gated R+1==A: timers 283->250
# with 1xs -36 fixed but 16xs +18 accumulation overshoot and 5 lib
# HW-fit pins red, proving ties symmetric and per-tie delays
# accumulate in long runs), slow line-delay +2 as nIRQ synchronizer (timers 283->219 with matrix
# 158/94/0 and pins intact, but prescaled +1 overshoot and value churn;
# +1:+3/+4 landscape 265/316/358 proves it fitted, not HW). Tri-proof:
# the race needs service +2 while D forbids +2, and no path component
# can compensate (all pinned/exact), so no consistent fix exists here.
# Hypothesis: our fetch/expand phase lags the HW 3-stage pipeline by
# ~1-2T at the sample boundary, so our service-first order loses ties
# HW wins by completing the in-flight instruction; fixing needs a
# fetch-2-ahead remodel with started-aware entry (deferred). Do not
# retune start/line/prologue/IO charge to chase these cells.
# 2026-09-18 session (baseline 283 re-confirmed deterministic over 3
# runs; a stale binary briefly showed 250, discarded): early
# timer-step (timers -27 true, but timer_irq -28: the write-shift
# breaks start), counter-read peek (timers no-op, timer_irq -90:
# HW samples reads pre-edge), slow line-delay +2 re-measured
# (timers -64 true, but timer_irq -21 plus 6 lib observation-shift
# red: entry +0 is HW-exact). All reverted. mgba timer_irq is the
# entry gate: every start/line/entry shift is vetoed by ROM, and
# retire-gated stays reverted (per-tie +1 accumulates: fitted).
# fetch-2-ahead increment E (multi-cycle retire defers the take 2T):
# timers 298 (+15: fixed 30, broken 45) with timer_irq -36, so takes
# must stay immediate; deferral lengthens windows (+1 counts) the same
# fitted way per-tie delays do. Reverted.
# fetch-2-ahead increment F (loop-aware take-defer via retire-PC ring,
# spin vs progressing-loop separated): timers TIMEOUT (no END:
# forward-progress break), timer_irq -89, irq-delay -2/2. Every ROM
# loops, so loop shape cannot separate takes; reverted.
# H-series (restart-reload): H wake-ack (timers 627, timer_irq -36),
# H2a any-restart-reload (timers churn net-0, nba_reload + timer_irq -36),
# H2b changed-gate and H2c 32-bit-reload-only (both timers no-op,
# timer_irq -36). Restart-keep is HW (timer_irq+nba prove it); a harness
# reproduced our Got=2 timeline (stale take + setup-residue take), but
# long cells demand full windows, contradicting every residue/stale
# combination. 283 is Pareto-optimal: every single move breaks a ROM
# table and combined moves cancel inside the entry total.
# K (STM internal +1 both engines, GBATEK questioned): timers 265
# (-18 net, values-churn) but timer_irq -36, irq-delay -1, Timing -60,
# sio -4 (all ROM-embedded vetoes). GBATEK STM stands Timing+nba
# validated; slower-master timers gain was fitting. Reverted.
# L (HLE SWI 02/04/05 +8T SVC-exception): timers no-op (wait absorbs),
# timer_irq -36 (HALT charge pinned). Post-swi grid moves are absorbed
# or harmful (G wake-40: +122). Reverted.
# M (enable stale-tick removal): timers no-op, timer_irq same 36,
# tick-before-reload -1 (stale tap-gated overflow is HW, Alyosha/nba
# pinned). Stands. Trace (timer_irq FFFF, IWRAM code, 1T nops): live
# reads exact, freeze ours 82/83 vs HW 81; strh-0 clobber lands after
# ovf#1 (double overflow) and uncoalesced 2nd IRQs stall versions
# (~+115T stale-disables, transition 2 versions early).
# N13 (32-bit enable start-delay 2->3, 2-phase bus): timers churn
# (fixed 36, broken 27), timer_irq +51, nba_reload -1. Enable phase is
# exact for both widths; width splits vetoed. Reverted.
# N14 (restart on any enabled->enabled CNT_H write): timers 0/0
# (length-preserving, both window ends shift), nba_reload -1 (32-bit
# same-value overwrites pin stale-continue). Reverted.
# N14b (restart only on value-changing rewrites): timers 0/0, all gates
# green. No benefit; 2-emu consensus is stale-continue. Reverted.
# N15 (control visibility +1T lag): timers 0/0, all green. Reverted.
# N15c (visibility on 8T boundaries): counts worse (0x0001 Got 1 vs 2),
# suite timeout (stuck halted-IntrWait with dead prime after 16.2M of
# 100M), values scatter. Reads-side lag breaks suite invariants
# (stale images cause first-sample exits and phase compression), so
# control reads stay immediate. Fully reverted; 283/936 restored.
# Trace campaign (0x0005 1d1i tight, IWRAM code): our window is
# enable-458, first-sample-461, ovf-465, disable-write-557 (92T entry),
# exit-sample-712 (155T exit path). Counts (r0, pre-counted) and values
# (frozen) are both invisible to the 155T exit path. Freeze 82-vs-81 is
# a STRH data-phase edge pinned by start-stop-8 on both sides.
# N20/N21/N22 (data-phase landing variants) all reduce to veto-certain
# or already-measured (N15) moves; skipped without runs. irq-delay-92
# is reset-anchored (composite), so it does not pin the entry alone;
# that separation is Phase-12 future work, not a current lever.
# Storm model (both suites' testIrq stops the timer without acking IF,
# so every boundary re-enters until the next prime-IntrWait clears IF;
# counts alone reject the clean single-entry model, which predicts
# ~12 samples where both sides show 2-3). r0 is the inter-entry gap
# count, so per-entry fitted time is the lever. N26 (prologue 23->22
# global): timer_irq 36->75 (+39) but timers 653->633 (-20), sio 0/4,
# lib -12. Reverted as global; the split (nested entries want 22,
# outer entries want 23) ships as N26b below.
# N26b (nested takes observe 22, outer keeps 23): timer_irq 36->54
# (+18), timers 653 unchanged, Timing 2020, sio 4/4, nba timers green,
# lib 357 green, full matrix 6822/7288. Landed (cpu/mod.rs).
# Storm trace (temp GBA_STORMTRACE, pair-9 tight): 39 sequential outer
# takes (~113T each, all nested=false), 3-6 per tight window; the loop
# makes no progress between return and re-take (IF never acked, every
# boundary re-enters). r0 is pre-take#1 samples plus the post-storm
# exit: take#1 at the first post-s1 boundary preempts s2 (Got 2).
# N32 (timer0 take deferred within 12T of a fresh enable, IF stays):
# timers 653->656, timer_irq 54->65 (+11 frontier pairs), all gates
# green. The delay lets s2 sample before take#1 (0x0005 1xs x9 fixed).
# N32b (defer only while channel-0 overflows <= 2): heals the 0x0001
# period-1 regressions (established-rate takes stay immediate), keeps
# all N32 gains: timers 665 (+12), timer_irq 65 (+11), Timing 2020,
# sio 4/4, nba green, lib 357, matrix 6843/7288. Landed.
# N32c (second-overflow takes observe window 20): no-op for the ii2
# freeze target, moved ii4 cells the wrong way (freeze phase is a
# per-version lottery, not a uniform knob). Reverted.
# N33 (ovf-locked one-slot defer of first-test takes): fixed the C2
# dd2 cells and many 0b loose cells but overshot the paired tight
# cells (+1) and other phases (timers 665->651, timer_irq 65->69):
# tight and loose want opposite take timings (structural trade, no
# uniform gate exists). Reverted; N26b+N32b (timers 665, timer_irq
# 65, all gates green) stands as the landed set. Remaining timers
# residual (271) is phase-lottery/structure: C2 dd2 (enable half-slot
# grid phase), 16xs (23T loose-slot steals), values (stop-timing mod
# period), ii2-16xv freeze (per-version lottery).
# N34 (defer window 12->14 for the uniform -2 freeze phase): fixed
# the ii1 loose values but broke the ii4 loose values (take#1 moves
# the whole storm, so ii4 follows: causal lock). Reverted.
# N35 (stop-only enable-bit +1T read lag): zero flips anywhere. The
# freeze is set at stop time (counter halts), so delaying the exit
# observation cannot move it: misdesigned. Reverted.
# N36 (timer-mask line +1T): timers suite dead (0/0, prime-IntrWait
# halt-wake desync), unit IF-timing test red. The line pipeline is
# halt-coupled and untouchable. Reverted; N32b restored (665/65).
# N37 (ovf#2-locked defer): broad +1 churn (timers 665->618), ii2
# target unhealed: the ovf-count gate fires everywhere except where
# needed. Reverted.
# N37b ([8,16) ovf#2 window): no-op (take#2 arrives past it or the
# stop-take carries ovfs==1, not 2). Reverted.
# N37c (ovfs==1 late-take past the enable window): no gain, broke
# alyosha_halt_pc_4 (take deferral interacts with halt/wake). Fully
# reverted including the last_ovf_cycle tracking.
# N38 (IF-up return +8T): fixed ii2 values but broke ii4 values
# (return shifts move all freezes: trade). Reverted.
# N38b (ovfs-gated return extra): never fires (clean returns are
# IF-down: stops hold, all takes are fresh-ovf clean, no storm).
# Fully reverted. Standing model: clean takes per fresh overflow,
# stops hold; r0 = pre-take samples + post-stop exit. Landed set
# remains N26b+N32b (timers 665, timer_irq 65, all gates green).
# Phase probe (temp): all C2 test-enables land at prescaler mod64=50
# (version-invariant). The d1-only -1 is a grid steal (take absolute
# fixed, loop grid +4T from the delay loop), not enable phase; no
# take-time-visible gate splits d (unwinnable without phase fitting).
# Documented; C2 stays.
# T3 (take#1 defer relative to first overflow, ovfs<=2 gated): timers
# 665->763 (+98), timer_irq 65->69 (+4), Timing 2020, sio 4/4, DMA 1244,
# nba timers/irq/DMA green, lib 357 green, matrix 6946/7288 with no new
# unexpected case (cancel_ime kept passing). Replaces the enable-window
# defer (longer reloads overflow past it); established-rate and
# pre-enable stale takes stay immediate. Atomic 32-bit enables sample
# later (5T) than split CNT_L+CNT_H enables (3T). N26b nested split
# reverted (no-op under T3: takes never nest here, scores identical).
# Post-T3 audit (timers 763, timer_irq 69): native take timeline
# (enable@E, ovf#1@E+1..3, read, dispatch, stop-write, freeze) shows
# dispatch aligned with HW inside the live/frozen race window (K-curve:
# K<=4 breaks live diagonal cells, K>=5 saturates), while the frozen
# cells sit uniform +3 with storm-independent single takes. Vetoed with
# ROM gates, all reverted: same-tick stop (nba start-stop 1/2),
# device fast-line (HW-anchored edge units red), discarded-fetch removal
# (no frozen move; irq-delay 1/2), entry 23->20 (timer_irq 90/90 but
# storm 645 and irq-delay 1/2: the -3 sits on take-path length with
# dispatch aligned, and no uniform (entry,K) serves storm grid and
# single takes jointly). Superseded by T4 below (class-gated -3).
# Take-path audit (temp probes, all reverted): FFFF take is OVF+5..7,
# entry 23+0 (raw entry_bus exactly 0, no clamp effect), dispatch +
# testIrq-to-stop 56, every instruction GBATEK-textbook (IWRAM
# single-cycle leaves no bus wiggle). Dispatcher binary identical
# across suite/nba ROMs (one word differs: table pointer); testIrq is
# 8 Thumb instrs (TM0+TM1 stop + flag); nba handler is all-IWRAM (no
# EWRAM cancellation). K4 gate search (fresh+ovfs<=2+atomic+ps0 entry
# -3): timer_irq 90/90, storm +15 net, nba green; the halt_pc_4 veto
# (same take class pins full entry 23) resolved by the wake exclusion
# (5th condition, ablation-proven). Landed as T4 below.
# Stop-latency moves conflict with the start-stop 2ND=8 pin. Next
# lead: downstream take#2+ phase (unmasked by T4, see below).
# T4 (boundary atomic-fresh-ps0 timer0 takes enter 3 cheaper):
# timer_irq 69->90/90 GRADUATED (box removed), timers 763->778/936
# (+87 fixed, -72 churn, net +15). The 87 fixed all missed by uniform
# ~3/~2 (absolute-path error, now exact); the 72 broken are all 4i
# cells missing by +-1..3 phase scatter. Gate (all five ablation-proven
# load-bearing): timer0 IF up + ovfs<=2 + atomic 32-bit enable (fresh
# reload) + prescaler 0 + non-wake take. Drops vetoed by ROM:
# no-IF (unrelated takes), no-count (E1: storm 775, 4i worse),
# no-fresh (nba irq-delay split enables break 1/2), no-ps0 (storm
# ps1024 rows collapse to 654), no-wake-exclusion (alyosha_halt_pc_4
# breaks: same take class pins full entry 23). E2 (ovfs<=1) undercovers
# double-wrapped take#1s (timer_irq 84). E6 (epilogue 7->6) destroys
# sio_timing 0/4. Fire-map: timer_irq 90 + timers 270 + ~23 benign
# single fires in alyosha micro-tests (setup takes, score-neutral);
# halt/nba/timing/sio never fire. Partition: fixed = 78x1i + 9x2i,
# broken = 72x4i; baseline 1i failed 79 (now ~0), so take#1 -3 is
# absolute-correct and the 4i churn is downstream take#2+ phase error
# unmasked by the fix (2i now miss by +-1) - the lead for Phase 12.
# Standing model: T3+T4+entry 23; mechanism open.
# T4 ablation: dropping the IF gate discounts unrelated takes (E1
# without count: storm 775, worse); dropping count (E1) or fresh
# (nba 1/2) or ps0 (storm 654) or wake (halt_pc_4) each regresses.
# E2 (ovfs<=1) undercovers double-wrapped take#1s (timer_irq 84).
# E6 (epilogue 7->6) destroys sio 0/4; E8 (7->8) fixes storm +101 but
# breaks sio the other way, so the shared return path is exact and
# the residual lives in take#2+ handling, not the return.
# T5 (established re-takes, ovfs>=3, enter +1): storm 778->901/936
# (+123). 2i values ran systematic -1 with T4-only (take#2 a tick
# early); first takes keep T4/full entry, non-timer0 takes never match
# (timer0 IF required), sio/nba/halt/timing all green. Residual 35:
# slower ps0 rows (0x0040+, take#2 at ovf#2 with count 2 stays T4-kept)
# and 10b/8b/6b cascade-bit rows; mechanism open (re-take entry vs
# re-raise pipeline undistinguished - both shift take#2 +1).
# T6 (2nd-overflow raisings from a down IF apply 2 ticks later):
# storm 901->910/936 (+9, 0 broken). Slow-row take#2 (ovf#2-anchored,
# count==2, T4-kept) ran -2; entry moves in 3s so only a position +2
# reaches exact, and +1 (T6b) merely moved -2->-1. count==2 (not >=2:
# that broke 15 slow 4i cells) with the T4 class guards keeps nba
# (count 1), halt (count 1), timer_irq doubles (IF still up) and fast
# return-anchored takes (line up early) untouched.
# T7 (missed-one takes complete take+entry at raise+25): storm
# 910->916/936 (+6, 0 broken). Fast-row take#2 answers the 3rd
# overflow second (2nd arrived masked, discarded; exactly one ack on
# record) with grid-split latencies 5/3; entries 20/22 absorb the
# sampling delay (take+entry const). Clean takes keep T4/T5, fast
# rows (counts 10+) and slow take#3 (two acks) never match.
# Residual 20: slow-4i loose takes (+2 grid phase, device-blind
# width-split entries) and cascade-bit rows (+1iter detection-boundary
# luck with value-blind stops); mechanism open.
# 2026-09-20 session (baseline 916/936 re-confirmed; all TEMP probes
# reverted): residual decomposed as sub-iteration take-vs-poll races
# (3-7 tick margins). Slow-ps0 4i (0x0080/0x0800 sums+freeze,
# 0x8000 freeze-only): take#4 is raise-bound (514) and lands 7 ticks
# after the loose idx6 poll (511, up) while tight needs its idx19 poll
# (510, up): the 1-tick window is empty, so no uniform take/entry/grid
# move serves both (take#3 +7..17 loose-eaten vs +0..6 tight-executes;
# take#4 -3 loose-freeze vs -0 tight-freeze; iters/returns split the
# same way; verified by TEMP take/poll/raise/ret/pc/entry_bus timeline
# probes). Prescaled 4i/1i sums-only (6b 4d4i, 8b 2d4i x2, 10b 2d4i,
# 10b 1d1i; values exact): tap-phase delay-lottery, takes landing 3-5
# past the critical poll. DIAG, all TEMP and reverted: D1C (slow-clean
# take#3 (ovfs==3,acks==2) +10 uniform) fixed loose-4i sums and broke
# tight-4i sums (run-dependence proven empirically, gates green);
# D1B (+ loose-PC-gated take#4 (ovfs==4,acks==3) at 25-lat) reached
# 931/936 (+15 ceiling; PC-gating is per-ROM fitting, not landable);
# D2 (T3 prescaler-0-only) broke prescaled 1i takes (T3 needed there);
# D3 (timer0 class-gated return +1) churned fast-row pended-take stops
# (794/936, -122). 6b-4d-4i runs 7 takes (3 post-exit stale-IF re-takes
# at ROM PCs; sum sealed at loop-exit). Follow-up (take#4 25-lat deep
# dive, probe logs only): 25-lat is exact for all 9 loose freezes
# (0080 lat4->21, 0800/8000 lat3->22) but uniform use breaks all 9
# tight freezes (same lat3 needs T5+1=24; net-zero churn, sums
# untouched) - tight wants 25-lat+2 uniformly, so even values need a
# run-gate; PC address is the only splitter (forbidden). T8
# (late-sampled clean take#4 (ovfs==4,acks==3) lat>=4 only -> 25-lat):
# storm 916->919/936 (+3/-0, 0080-loose-16xv fixed, all gates green).
# The lat>=4 gate fires only there (0800/8000/tight take#4s sample at
# lat3 and keep T5; fast rows miss (acks==1) and keep T5/T7; prescaled
# and wake takes never match). T8b (same class at lat3 interrupting a
# branch (in-flight opcode class, never addresses) -> 25-lat): storm
# 919->922/936 (+3/-0, 0800-loose-16xv fixed (BNE-interrupt take#4s get
# 22), all gates green). Per-cell forensics (TEMP store/take/poll probes,
# all reverted; runTest is IWRAM code so takes land at IWRAM loop PCs):
# 0080-loose take#4 interrupts ADD#1 (lat4, T8), 0800-loose interrupts
# BNE (lat3, T8b->22), 8000-loose interrupts ADD#12 (lat3, keeps T5=24);
# tight take#4s never interrupt BNE (ADD/LDR/TST only). Poll trace proves
# 0800-loose runs one extra pre-take#4 UP-poll (342 vs 339+exit): the
# take#4 position is raise-anchored, so the +1 is grid phase. T9 DIAG
# (clean take#3 (ovfs==3,acks==2) interrupting full-simple-pipe +24,
# full-iter phase-preserving grid shift): fixed 0800 sums AND kept 0800
# freezes (take#4 stays on BNE) but broke 8000 sums (were passing):
# reverted. Per-cell proof 0800-take#3 and 8000-take#3 are identical in
# every blind dimension (lat3, (3,2), full-pipe), so no uniform take-side
# grid fix exists; 0800 sums stay lottery-phase. T8c (same class at lat3
# interrupting full-simple-pipe (both in-flight opcodes single-cycle
# ALU) -> 25-lat): storm 922->925/936 (+3/-0, 8000-loose-16xv fixed
# (ADD-interrupt take#4s get 22), 8000 sums held, all gates green).
# Tight take#4s never match (an LDR or branch always breaks their pipe).
# T8-PS-blind DIAG (drop the ps0 gate): zero movement (prescaled sums
# are not take#4-entry-sensitive); reverted. Prescaled forensics
# (per-cell takes+polls, all TEMP reverted): residual loose takes share
# classes with passing twins (10b0012-1d1i take#1 lat7 (TST,BNE) vs
# tight lat5 (ADD,LDR) at base-23 both would need a run-gate; 4i cells'
# take#3s overlap passing-loose classes); prescaled takes are otherwise
# ungated (all T-gates ps0-only) yet 100+ pass at base, so the 5 are
# tap-phase lottery like the slow sums. 0080 forensics (8 polls, all 4
# takes ovf-separated and clean (acks 0-3), take#4 stops normally):
# freeze exact (stop pinned) with sums +1, so the grid is one poll ahead
# while the take#4/stop is exact; loose-take#1 (lat6 (TST,BNE)) vs
# tight-take#1 (lat5) at T4=20 both would need a run-gate. Mechanism
# open. Standing: 925/936; residual 11 are phase-lottery beyond the
# uniform model (needs run-gated grid terms). T10 (clean take#3
# (ovfs==3,acks==2) interrupting just before a transfer (in-flight
# simple ALU, transfer next), ARM ps0 fresh only -> +24 (full-iter
# phase-preserving grid shift)): storm 925->928/936 (+3/-0, 0080-loose
# sums fixed (take#3 (ADD,LDR) gets +24, take#4 stays on ADD#1/lat4 via
# T8 so freezes hold), all gates green). Unscoped T10 broke 20 tight
# prescaled 1xs (tight ADD-takes share the (ADD,LDR) pipe: the pattern
# is not loose-specific, only twin-safe where tight avoids it (ps0
# slow take#3s)); reverted to ps0 scope. 0080/0800/prescaled twins all
# proven (same take classes, different grid wants); 0800 sums and
# prescaled 5 stay lottery-phase. Standing: 928/936; residual 8
# (0800-16xs x3, prescaled-16xs x5). T11 (clean (3,2) full-pipe take#3
# + take#1-lat==5 history -> T5+25: middle total one iter longer):
# storm 928->931/936 (+3/-0, 0800-loose-16xs sums fixed (take#4
# phase-preserved via T8), all gates green). +24 overshot the freeze
# by 1 (one loose iter is 25c: take#4 fell to lat2/T5); HLE+26 holds
# both. Gate matches only 0800-loose (survey-proven: 8000 take#1s at
# 7, 0080 at 6, prescaled excluded by ps0 scope, tight loops cannot
# hold (ADD,ADD)). Take#2 keeps T5 (2i freezes pin its disable).
# Standing: 931/936; residual 5 (6b0013-4d4i, 8b0010/8b0012/10b0010
# -2d4i, 10b0012-1d1i, all loose sums +1 iter, freeze-blind).
# Prescaled-5 forensics (TEMP take/enable/stop/LDR probes, all
# reverted; loose iter is 23c): all 5 fail with the disabling take at
# loop-bottom (TST,BNE, 3c after the last LDR), so handler resume
# re-takes the branch and runs a full extra iter (+0x10); passing
# twins' disabling takes land mid-loop (partial final, same totals).
# Mechanism: lengthening a middle-take handler by ~1 iter drops one
# pre-take LDR at the grid-fixed final take (phase-preserved: take
# keeps pipe/lat/off). Proven by TEMP T12 (prescaled take#3 +23,
# tight-pipe-excluded, reverted): fixed 6b0013-4d4i and 8b0010-2d4i
# (-0x10 exact), broke ~29 (ADD,ADD)/(TST,BNE)/(BNE,STR)-take#3 passes
# with -0x10, zero tight breaks, timer_irq 90/90. This also explains
# the T8-PS-blind zero movement (entry costs never move takes; only
# take position matters). Non-scopeability is twin-proven: every
# refinement breaks a passing twin ((ADD,ADD)+take1lat7 breaks
# 10b0012-1d4i; (BNE,STR) breaks its twin; (ADD,LDR) breaks 6b0012-1d4i
# and all 36 tight-4i), and no uniform fractional shift can thread
# the bottom zone (passing take-offs cover 22/23 phases). 10b0012-1d1i
# is unfixable in-model (single take at bottom: lat floor, and the
# stop time is irrelevant since the resume path is fixed). Verdict:
# history-phase lottery (take position = free-running grid phase vs
# loop phase; ours lands bottom where HW lands mid). Do not refit;
# needs cycle-exact history, not take gates.
# Phase-lock (2026-09-20 forensics, TEMP probes reverted): prescaler
# phase at test enables is globally locked to phi(d)={178,183,193}
# (all 26 cells; 390/467 inter-enable gaps =0 mod 1024, rest +5/+10/-15
# = delay steps; boot phase washed out). Takes are deterministic
# F(cell,d,i), so this is not a history lottery: 5 deterministic
# divergences at locked phases. Unfixability axes (every axis shared
# with passes): phi (all three values), P/N/ii/reload (all shared),
# take features (twin-proven pairs), run index (locked, no history),
# loop code (uniform moves interlock: bottom cluster off3-4 plus mid
# cluster off7-8 flank every fractional delta). Architecture recheck:
# free-running divider tri-confirmed (NBA/ares/Mesen + GBATEK silent +
# the lock itself refutes reset), start_delay 2 + stale-tick edge match
# all three refs, NBA first-overflow matches within 2c (+P edges only,
# wrong magnitude for +1-iter fails; NBA-adopt predicts net -7).
# Ceiling 931/936 barring HW-measured grid-rule edge data; do not
# refit with gates or uniform shifts.
# T12 (prescaled take#3 at lat 3 outside tight pipes -> +2: take#4
# resampling): storm 931->935/936 (+4/-0, all four prescaled-4i loose
# sums fixed, all gates green). The +2 middle lengthening re-quantizes
# take#4 sampling non-rigidly (takes do not move rigidly with the loop:
# lat re-quantizes per position, so in-scope passing twins hold while
# bottom takes exit at the LDR). Scope: (ADD,ADD)/(BNE,STR)/(TST,BNE)/
# (ADD,CMP) take#3s at lat 3, plus (ADD,LDR) at lat 3 with take#1-lat 5
# (tight (ADD,LDR)s are lat 5/4 or take1lat 6: structural). Bracket:
# +1 fixes only the (BNE,STR) cell (932), +3 breaks 8b0011-4d4i (934);
# +2 is the sweet spot. 10b0012-1d1i has no take#3 (single take at
# bottom: lat floor, stop-irrelevant, immovable). Standing: 935/936;
# residual 1 (10b0012-1d1i loose sums +1 iter). Mechanism open.
# 1d1i verdict (TEMP take/poll/stop/ack/ret probes + 216-take
# prescaled survey, all TEMP reverted): the +0x10 is a post-resume
# iter, not take cost. Last UP poll 3c before take#1 (TST,BNE, lat7,
# base 23); handler ack+67/stop+89/ret+113 matches the passing tight
# twin (take#1 lat5 (ADD,LDR), resume re-polls down), so resume
# re-runs the stale branch for one extra iter. HW takes 1-4c earlier
# with the final LDR in flight (resume re-polls down, no post ADDs).
# Unreachable: takes fire only at instruction steps; the final-LDR
# step (ovf+3) is blocked (line up just then but defer open till
# ovf+5) and the next step (TST, ovf+7) is past the writeback. The
# fail is the unique single-take race-19 post-writeback take (same
# spot take#1s in 1d2i/1d4i pass: last take wins; same-race tight
# twins pass pre-LDR). Single-take races are rigid (grid moves take
# and poll together; entry costs are post-take), uniform O/line/
# defer/anchor moves shift all ~100 brittle loose races, and per-cell
# gates are forbidden. Needs HW edge data, not model refits.
# E1 (prescaled-fresh defer window 5->1, TEMP reverted): storm
# 935->919/936 (+1/-17, timer_irq 90/90). The +1 is 10b0012-1d1i
# (take fires at ovf+3 on the LDR step now that defer closes: line
# was never the blocker). The -17 (eleven tight-2d1i 1xs takes moved
# -2, five loose take#1s out of window/T12-mismatch) proves the same
# class needs window 5: no blind signal at the decision point splits
# it (tight LDR-steps share entry/lat/ovf/acks/ps/fresh; narrowing to
# lat/pipe/time slices is fitting). Window values 1-3 are identical
# (line binds at ovf+3), 4+ never fires pre-writeback: 5 is the
# unique optimum. Compensation is impossible (r0 seals at
# take+resume; no post-hoc lever) and core-remodel sampling keeps
# ARM7 completion (same takes). Residual stands 1, in-model exact.
# E2 (IRQ avail/line pipeline -1/-1 globally i.e. line -2, TEMP
# reverted): storm 935->890/936, timer_irq 90/90, nba_irq_delay 2/2.
# The veto on line moves comes from storm's own established takes
# (slow-ps0 rows break in sums+values: T5/T7/T8 ecosystem tuned with
# line at ovf+3), not timer_irq/nba (their takes are defer-bound, so
# they hold). Ours unfixed (defer still binds at ovf+5). Take
# position is fully pinned: allowed must jump 700-><=696 (values
# 697-699 are a dead zone: same TST take by quantization) but any
# >=4 jump breaks dense tight takes, grid moves break tights either
# way, and the cliff cannot cross the fixed take (needs unphysical
# LDR length).
# Ref triangulation (primary sources, no TEMP needed): NBA first
# tick is 2c later than ours (control +2 scheduler events,
# StartChannel(offset-1)); Mesen matches EnableDelay 2 + stale edge
# but taps at &mask==0 (1c later); ares taps at &mask==0 (1c later,
# no enable delay); GBATEK silent on start latency. Refs span +-2
# (first-tick uncertainty band confirmed) but the fix needs -2 early
# of all refs (O-2) or window<=3 (E1-blocked): no ref supports either
# direction or magnitude. Avenue closed.
# E3 (window-1 + timer-STR +2 stall combo, TEMP reverted): storm
# 935->663/936, timer_irq 90->15/90. Ours fixed (take at shifted
# LDR step pre-writeback) but grid moves break absolute-counter
# values and re-quantize takes (tight sums die; loose sums hold on
# plateaus). Lesson: grid is pinned absolutely (values + tights),
# so the fix direction (take pre-writeback) is valid only without
# moving the grid, which E1 already proved unscopable. Combo dead.
# Close-out (no TEMP; derivation audit): sequential global fixes
# commute to one net move (same dead landscape); multi-take healing
# is automatic (grid +-23 flips take#2 exactly, no control lever);
# LDR-lengthen drifts periods (fatal) or needs vetoed compensation;
# 30-item audit (nesting/DMA/stale-IF/sync/prefetch/entry/prologue/
# spurious-take/branch-decode/ALU) all clean or pinned. Hypothesis
# space exhausted; ceiling 935/936 stands pending HW edge data.
# E4-equivalence (no build needed): scoped line-bypass is vacuous
# (line already up at every firing check; E1 proved defer alone
# binds), so no scoped-line experiment exists beyond E1. Done.
# Functional completeness (audit, no TEMP): single-take r0 =
# f(take, grid) only (resume/stop/handler all take-determined;
# third inputs audited: none). Take/grid levers are 4 (line, defer,
# O, grid) and every integer form is landscaped (line 0-4, window
# 0-8, start 0-4, tap band, waits): only window<=3 fixes, always
# -17. No unmapped freedom remains.
# Dichotomy (audit): moves are rigid (take+poll together: r0
# invariant, useless) or split (take-vs-poll: pigeonhole over dense
# tights/cliffs). No third option in 1-DOF relative offset.
# VBlank-exit/frame deltas are uniform race shifts (same wall);
# frame exactness proved by 1-fail pattern (drift would scatter).
# mGBA comparison (0.10.5 binary headless + master source log):
# mGBA scores 639/936 (we lead by 296); it fails our cell too
# (both sums: its +7-event take without defer/gates is systematically
# late, 114 tight fails). Our defer-5 is HW-true globally by
# contrast. Upstream master has no take-timing fix (timer: rename +
# cascade-disabled only; no IRQ-delay/pipeline change), so both
# reference emus share the flaw: tables are HW-only, fix needs HW
# edge data, not model refits.
- mgba_suite_timers
# alyosha_gba-tests: settled R12 first-failure values (30M-cycle probe matches 3M). Timing/HW-accuracy backlog; do not refit.
# (DMA_Mode_Change graduated 2026-09-16 by keeping the CPU fetch stream
# across DMA CNT_H commits; matrix 157/95/0, lib 277 green.)
- alyosha_BL_IRQ
- alyosha_BL_IRQ_2
- alyosha_BL_IRQ_3
- alyosha_BL_IRQ_R14
- alyosha_bounday_test_1
- alyosha_dma_from_bios
- alyosha_DMA_pause_timing_end_1
- alyosha_DMA_pause_timing_end_2
- alyosha_DMA_pause_timing_end_3
- alyosha_DMA_pause_timing_end_4
- alyosha_DMA_pause_timing_mid_1
- alyosha_DMA_pause_timing_mid_2
- alyosha_DMA_pause_timing_ROM_to_IWRAM
- alyosha_DMA_ROM_Fixed
- alyosha_fifo
- alyosha_fifo_2
- alyosha_fifo_3
- alyosha_fifo_4
- alyosha_fifo_5
# fifo_6 (0x76 vs 0x7B): pre-15f291f1 "pass" was a false positive
# (SOUNDCNT_H routing gate blocked every sound-DMA fire, IRQ never ran,
# r7 stayed 0 so f001a reported r12=0). Gate removal unmasked the honest
# -5 (EWRAM32 sound burst + DMA-completion IRQ reads TIM1 5 early).
# No ref supports the +5 (Mesen/NBA unit costs match); do not refit.
- alyosha_fifo_6
- alyosha_fifo_dma_disable_4
- alyosha_halt_pc
- alyosha_halt_pc_2
- alyosha_halt_pc_3
# halt_pc_2/3 timer residuals (+1/-1) need opposite-sign fixes; no uniform
# entry/wake change fits both (verified). halt_pc lr split (0xB8 vs 0x74)
# across identical-layout binaries is not HW-explainable; do not refit.
- alyosha_IE
- alyosha_IF
- alyosha_IF_Timer
- alyosha_Internal_Cycle_DMA_IRQ_Br_pre_tim
- alyosha_Internal_Cycle_DMA_Mul
- alyosha_Internal_Cycle_DMA_TIM_Br_pre
- alyosha_IRQ_sub
- alyosha_IRQ_sub_slow
- alyosha_prefetch_enable
- alyosha_prefetch_enable_2
- alyosha_prefetcher_boundary_1
- alyosha_prefetcher_boundary_2
- alyosha_prefetcher_boundary_4
- alyosha_prefetcher_branch_thumb
- alyosha_prefetcher_branch_thumb_2
- alyosha_prefetcher_branch_thumb_3
- alyosha_prefetcher_branch_thumb_4
- alyosha_prefetcher_branch_thumb_5
- alyosha_prefetcher_branch_thumb_arm
- alyosha_prefetcher_branch_thumb_arm_3
- alyosha_prefetcher_branch_thumb_arm_4
- alyosha_prefetcher_dma
- alyosha_prefetcher_full_arm
- alyosha_prefetcher_full_arm_2
- alyosha_prefetcher_full_thumb
- alyosha_psr
- alyosha_serial_time
# serial_time +8 (0xA0 vs 0x98): EWRAM-data prepay removal moves it to
# 0x9B/0x95 but breaks cpy_data_bios and 128kb-boundary; full removal
# undershoots to 0x93. Residual is in prefetch-overlap around EWRAM-busy
# windows (comparison §4 area). mgba sio-timing (transfer + setup/exit)
# passes, so the SIO engine itself is correct.
- alyosha_serial_time_2
- alyosha_serial_time_2M
- alyosha_serial_time_2M_2
- alyosha_serial_time_2M_3
- alyosha_serial_time_3
- alyosha_serial_time_start_bit_2
- alyosha_Sprite_Last_VRAM_Access
- alyosha_Sprite_Last_VRAM_Access_Free
- alyosha_start_up_vbl
- alyosha_timer
- alyosha_timer_disable
- alyosha_timer_reset
suites:
- name: jsmolka_gba-tests
  cases:
      # These aggregate ROMs leave the first failed test number in R12/R7.
      # Zero therefore means that every embedded instruction test completed.
  - id: jsmolka_arm
    rom: arm/arm.gba
    cycles: 1000000
    verify:
      registers:
        r12: 0x00
  - id: jsmolka_thumb
    rom: thumb/thumb.gba
    cycles: 1000000
    verify:
      registers:
        r7: 0x00
  - id: jsmolka_memory
    rom: memory/memory.gba
    cycles: 1000000
    verify:
      registers:
        r12: 0x00
  - id: jsmolka_bios
    rom: bios/bios.gba
    cycles: 2000000
    verify:
      registers:
        r12: 0x00
  - id: jsmolka_dma_nes
    rom: nes/nes.gba
    cycles: 2000000
    verify:
      registers:
        r12: 0x00
  - id: jsmolka_ppu_hello
    rom: ppu/hello.gba
    cycles: 1000000
    verify:
      frame_pixels:
      - {x: 0, y: 0, color: 0x0000}
      - {x: 73, y: 77, color: 0x7FFF}
      - {x: 80, y: 76, color: 0x0000}
  - id: jsmolka_ppu_shades
    rom: ppu/shades.gba
    cycles: 1000000
    verify:
      frame_pixels:
      - {x: 0, y: 0, color: 0x0000}
      - {x: 16, y: 0, color: 0x0800}
      - {x: 32, y: 0, color: 0x1000}
  - id: jsmolka_ppu_stripes
    rom: ppu/stripes.gba
    cycles: 1000000
    verify:
      frame_pixels:
      - {x: 0, y: 0, color: 0x560B}
      - {x: 8, y: 0, color: 0x6290}
  - id: jsmolka_save_none
    rom: save/none.gba
    cycles: 1000000
    verify:
      registers:
        r12: 0x00
  - id: jsmolka_save_sram
    rom: save/sram.gba
    cycles: 1000000
    verify:
      registers:
        r12: 0x00
  - id: jsmolka_save_flash64
    rom: save/flash64.gba
    cycles: 20000000
    verify:
      registers:
        r12: 0x00
  - id: jsmolka_save_flash128
    rom: save/flash128.gba
    cycles: 20000000
    verify:
      registers:
        r12: 0x00
- name: alyosha_gba-tests
  case_patterns:
      # Fork of jsmolka gba-tests; same R12-first-failure convention.
      # Zero means every embedded test passed.
  - glob: '**/*.gba'
    exclude_globs: ['unsafe/**']
    id_prefix: alyosha_
    cycles: 3000000
    skip_screenshot: true
    verify:
      registers:
        r12: 0x00
- name: nba-emu_hw-test
  cases:
  - id: nba_dma_start_delay
    rom: dma/start-delay/start-delay.gba
    cycles: 3000000
    verify:
      memory:
      - {address: 0x030001C4, value: 1, width: 4}
      - {address: 0x030001C8, value: 1, width: 4}
  - id: nba_dma_latch
    rom: dma/latch/latch.gba
    cycles: 3000000
    verify:
      memory:
      - {address: 0x0300014C, value: 4, width: 4}
      - {address: 0x03000150, value: 4, width: 4}
  - id: nba_timer_reload
    rom: timer/reload/reload.gba
    cycles: 3000000
    verify:
      memory:
      - {address: 0x03000424, value: 7, width: 4}
      - {address: 0x03000428, value: 7, width: 4}
  - id: nba_timer_start_stop
    rom: timer/start-stop/start-stop.gba
    cycles: 3000000
    verify:
      memory:
      - {address: 0x03000264, value: 2, width: 4}
      - {address: 0x03000268, value: 2, width: 4}
  - id: nba_timer_tick_before_reload
    rom: timer/tick-before-reload/tick-before-reload.gba
    cycles: 3000000
    verify:
      memory:
      - {address: 0x03000284, value: 2, width: 4}
      - {address: 0x03000288, value: 2, width: 4}
  # --- additional nba-emu tests (Phase 8/8.5 - PPU/DMA/Bus/IRQ) ---
  - id: nba_irq_delay
    rom: irq/irq-delay/irq-delay.gba
    cycles: 3000000
    verify:
      memory:
      - {address: 0x030003E8, value: 3, width: 4}
      - {address: 0x030003EC, value: 3, width: 4}
  - id: nba_haltcnt
    rom: haltcnt/haltcnt.gba
    cycles: 3000000
    verify:
      memory:
      - {address: 0x030004EC, value: 6, width: 4}
      # haltcnt: boxed. HLE phases need HW anchoring.
      - {address: 0x030004F0, value: 6, width: 4}
  - id: nba_ppu_bgpd
    rom: ppu/bgpd/bgpd.gba
    cycles: 3000000
  - id: nba_ppu_sprite_hmosaic
    rom: ppu/sprite-hmosaic/sprite-hmosaic.gba
    cycles: 3000000
  - id: nba_ppu_status_irq_dma
    rom: ppu/status-irq-dma/status-irq-dma.gba
    cycles: 3000000
  - id: nba_ppu_bgx
    rom: ppu/bgx/bgx.gba
    cycles: 3000000
  - id: nba_ppu_dispcnt_latch
    rom: ppu/dispcnt-latch/dispcnt-latch.gba
    cycles: 3000000
  - id: nba_ppu_greenswap
    rom: ppu/greenswap/greenswap.gba
    cycles: 3000000
  - id: nba_ppu_vram_mirror
    rom: ppu/vram-mirror/vram-mirror.gba
    cycles: 60000000
    verify:
      memory:
      - {address: 0x030003F4, value: 0x0A, width: 4}
      - {address: 0x030003F8, value: 0x0A, width: 4}
  - id: nba_ppu_ram_access_timing
    rom: ppu/ram-access-timing/ram-access-timing.gba
    cycles: 3000000
  - id: nba_dma_burst_into_tears
    rom: dma/burst-into-tears/burst-into-tears.gba
    cycles: 3000000
    verify:
      memory:
      # Graduated 3/3.
      - {address: 0x03000234, value: 3, width: 4}
      - {address: 0x070003FE, value: 0xEA00, width: 2}
      - {address: 0x070003FC, value: 0xFF24, width: 2}
      - {address: 0x03000238, value: 3, width: 4}
  - id: nba_dma_force_nseq
    rom: dma/force-nseq-access/force-nseq-access.gba
    cycles: 3000000
    verify:
      memory:
      # Counters follow this .gba's layout. Graduated 88/88.
      - {address: 0x0300014C, value: 2, width: 4}
      - {address: 0x03000150, value: 2, width: 4}
  - id: nba_bus_128kb_boundary
    rom: bus/128kb-boundary/128kb-boundary.gba
    cycles: 3000000
    verify:
      memory:
      - {address: 0x030003DC, value: 0x12, width: 4}
      - {address: 0x030003E0, value: 0x12, width: 4}
  - id: nba_archive_irq_cancel_ie
    rom: archive/irq/cancel-irq-ie/cancel-irq-ie.gba
    cycles: 3000000
  - id: nba_archive_irq_cancel_if
    rom: archive/irq/cancel-irq-if/cancel-irq-if.gba
    cycles: 3000000
  - id: nba_archive_irq_cancel_ime
    rom: archive/irq/cancel-irq-ime/cancel-irq-ime.gba
    cycles: 3000000
  - id: nba_archive_ppu_mode4
    rom: archive/ppu/mode4/mode4.gba
    cycles: 3000000
  - id: nba_archive_ppu_exact_timing
    rom: archive/ppu/exact-timing/exact-timing.gba
    cycles: 20000000
    verify:
      memory:
      # 6/7; DMA3-XFER stays out (+2, upstream phase).
      - {address: 0x0300062C, value: 7, width: 4}
      - {address: 0x03000630, value: 7, width: 4}
  - id: nba_archive_ppu_mode3
    rom: archive/ppu/mode3/mode3.gba
    cycles: 3000000
  - id: nba_archive_ppu_mode2
    rom: archive/ppu/mode2/mode2.gba
    cycles: 3000000
  - id: nba_archive_ppu_basic_timing
    rom: archive/ppu/basic-timing/basic-timing.gba
    cycles: 3000000
    verify:
      memory:
      - {address: 0x03000EA4, value: 6, width: 4}
      - {address: 0x03000EA8, value: 6, width: 4}
- name: armwrestler-gba-fixed
  cases:
  - id: armwrestler_arm_alu
    description: ARM ALU PART 1 instruction tests via menu item 0
    rom: armwrestler-gba-fixed.gba
    cycles: 4494336
    reference: refs/armwrestler_arm_alu.png
    inputs:
      - {cycle: 2247168, buttons: [start]}
      - {cycle: 3370752, buttons: []}

  - id: armwrestler_arm_alu_part2
    description: ARM ALU PT 2/MISC instruction tests via menu item 0
    rom: armwrestler-gba-fixed.gba
    cycles: 6741504
    reference: refs/armwrestler_arm_alu_part2.png
    inputs:
      - {cycle: 2247168, buttons: [start]}
      - {cycle: 3370752, buttons: []}
      - {cycle: 4494336, buttons: [start]}
      - {cycle: 5617920, buttons: []}

  - id: armwrestler_arm_ldr_str
    description: ARM LDR/STR instruction tests via menu item 1
    rom: armwrestler-gba-fixed.gba
    cycles: 6741504
    reference: refs/armwrestler_arm_ldr_str.png
    inputs:
      - {cycle: 2247168, buttons: [down]}
      - {cycle: 3370752, buttons: []}
      - {cycle: 4494336, buttons: [start]}
      - {cycle: 5617920, buttons: []}

  - id: armwrestler_arm_ldr_str_part2
    description: ARM LDR/STR PART 2 instruction tests via menu item 1
    rom: armwrestler-gba-fixed.gba
    cycles: 8988672
    reference: refs/armwrestler_arm_ldr_str_part2.png
    inputs:
      - {cycle: 2247168, buttons: [down]}
      - {cycle: 3370752, buttons: []}
      - {cycle: 4494336, buttons: [start]}
      - {cycle: 5617920, buttons: []}
      - {cycle: 6741504, buttons: [start]}
      - {cycle: 7865088, buttons: []}

  - id: armwrestler_arm_ldm_stm
    description: ARM LDM/STM instruction tests via menu item 2
    rom: armwrestler-gba-fixed.gba
    cycles: 8988672
    reference: refs/armwrestler_arm_ldm_stm.png
    inputs:
      - {cycle: 2247168, buttons: [down]}
      - {cycle: 3370752, buttons: []}
      - {cycle: 4494336, buttons: [down]}
      - {cycle: 5617920, buttons: []}
      - {cycle: 6741504, buttons: [start]}
      - {cycle: 7865088, buttons: []}

  - id: armwrestler_thumb_alu
    description: THUMB ALU instruction tests via menu item 3
    rom: armwrestler-gba-fixed.gba
    cycles: 11235840
    reference: refs/armwrestler_thumb_alu.png
    inputs:
      - {cycle: 2247168, buttons: [down]}
      - {cycle: 3370752, buttons: []}
      - {cycle: 4494336, buttons: [down]}
      - {cycle: 5617920, buttons: []}
      - {cycle: 6741504, buttons: [down]}
      - {cycle: 7865088, buttons: []}
      - {cycle: 8988672, buttons: [start]}
      - {cycle: 10112256, buttons: []}

  - id: armwrestler_thumb_ldr_str
    description: THUMB LDR/STR instruction tests via menu item 4
    rom: armwrestler-gba-fixed.gba
    cycles: 13483008
    reference: refs/armwrestler_thumb_ldr_str.png
    inputs:
      - {cycle: 2247168, buttons: [down]}
      - {cycle: 3370752, buttons: []}
      - {cycle: 4494336, buttons: [down]}
      - {cycle: 5617920, buttons: []}
      - {cycle: 6741504, buttons: [down]}
      - {cycle: 7865088, buttons: []}
      - {cycle: 8988672, buttons: [down]}
      - {cycle: 10112256, buttons: []}
      - {cycle: 11235840, buttons: [start]}
      - {cycle: 12359424, buttons: []}

  - id: armwrestler_thumb_ldm_stm
    description: THUMB LDM/STM instruction tests via menu item 5
    rom: armwrestler-gba-fixed.gba
    cycles: 15730176
    reference: refs/armwrestler_thumb_ldm_stm.png
    inputs:
      - {cycle: 2247168, buttons: [down]}
      - {cycle: 3370752, buttons: []}
      - {cycle: 4494336, buttons: [down]}
      - {cycle: 5617920, buttons: []}
      - {cycle: 6741504, buttons: [down]}
      - {cycle: 7865088, buttons: []}
      - {cycle: 8988672, buttons: [down]}
      - {cycle: 10112256, buttons: []}
      - {cycle: 11235840, buttons: [down]}
      - {cycle: 12359424, buttons: []}
      - {cycle: 13483008, buttons: [start]}
      - {cycle: 14606592, buttons: []}
- name: PeterLemon-GBA
  cases:
  - id: PeterLemon_HelloWorld
    rom: HelloWorld/HelloWorld.gba
    cycles: 1000000
  - id: PeterLemon_DCTBlockDecode
    rom: Compress/DCT/DCTBlockDecode/DCTBlockDecode.gba
    cycles: 1000000
    skip_screenshot: true
    verify:
      memory:
      # Plain S.16 IDCT of DCT[0]=950/DCT[63]=500 block, host-replicated
      # bit-exact (ARM MUL low-32 + ASR semantics).
      - {address: 0x02000000, value: 0x007A, width: 2}
      - {address: 0x02000022, value: 0x003B, width: 2}
      - {address: 0x02000042, value: 0x0031, width: 2}
      - {address: 0x0200007E, value: 0x007A, width: 2}
  - id: PeterLemon_FastDCTBlockDecode
    rom: Compress/DCT/FastDCTBlockDecode/FastDCTBlockDecode.gba
    cycles: 1000000
    skip_screenshot: true
    verify:
      memory:
      # AAN fast IDCT of DCT[0]=950/DCT[63]=500 block, host-replicated
      # bit-exact (32-bit wrap on every add/sub/mul).
      - {address: 0x02000000, value: 0x007B, width: 2}
      - {address: 0x02000022, value: 0x003D, width: 2}
      - {address: 0x02000042, value: 0x0032, width: 2}
      - {address: 0x0200007E, value: 0x007B, width: 2}
  - id: PeterLemon_FastQuantizationBlockDecode
    rom: Compress/DCT/FastQuantizationBlockDecode/FastQuantizationBlockDecode.gba
    cycles: 1000000
    skip_screenshot: true
    verify:
      memory:
      # In-place result: dequant words are overwritten by the AAN passes,
      # so only the final IDCT block is checked (host-replicated bit-exact).
      - {address: 0x02000000, value: 0x0003, width: 2}
      - {address: 0x02000022, value: 0xFFF3, width: 2}
      - {address: 0x02000042, value: 0x00B2, width: 2}
      - {address: 0x0200007E, value: 0x0006, width: 2}
  - id: PeterLemon_HUFF8Decode
    rom: Compress/HUFFMAN/HUFF8Decode/HUFF8Decode.gba
    cycles: 8000000
    skip_screenshot: true
    verify:
      memory:
      # 3K prefix proves the tree walk bit-exact.
      - {address: 0x02000000, value: 0x62714E0E, width: 4}
      - {address: 0x02000400, value: 0x190814E8, width: 4}
      - {address: 0x02000800, value: 0x4DCC51CC, width: 4}
      - {address: 0x02000C00, value: 0x1A581A59, width: 4}
  - id: PeterLemon_LZ77Decode
    rom: Compress/LZ77/LZ77Decode/LZ77Decode.gba
    cycles: 20000000
    skip_screenshot: true
    verify:
      memory:
      # Host-decoded BG.lz (GBATEK LZ77); 76800 bytes to EWRAM base.
      - {address: 0x02000000, value: 0x62714E0E, width: 4}
      - {address: 0x02009600, value: 0x10E90884, width: 4}
      - {address: 0x02012BFC, value: 0x1A541A54, width: 4}
  - id: PeterLemon_QuantizationBlockDecode
    rom: Compress/DCT/QuantizationBlockDecode/QuantizationBlockDecode.gba
    cycles: 1000000
    skip_screenshot: true
    verify:
      memory:
      # Dequant block at WRAM+128 (Q*DCTQ) plus S.16 IDCT at WRAM,
      # both host-replicated bit-exact.
      - {address: 0x02000080, value: 0x0260, width: 2}
      - {address: 0x020000A2, value: 0x0000, width: 2}
      - {address: 0x020000C2, value: 0x0000, width: 2}
      - {address: 0x020000FE, value: 0x0000, width: 2}
      - {address: 0x02000000, value: 0xFFEC, width: 2}
      - {address: 0x02000022, value: 0xFFE5, width: 2}
      - {address: 0x02000042, value: 0x00A9, width: 2}
      - {address: 0x0200007E, value: 0xFFFE, width: 2}
  - id: PeterLemon_SoundBRRMonoSingleShot
    rom: Sound/BRR/SingleShot/MONO/SoundBRRMonoSingleShot.gba
    cycles: 3000000
    skip_screenshot: true
    verify:
      memory:
      # 1K prefix proves the pipeline bit-exact.
      - {address: 0x02000000, value: 0x00000000, width: 4}
      - {address: 0x02000040, value: 0x00000000, width: 4}
      - {address: 0x02000100, value: 0x00000000, width: 4}
      - {address: 0x02000400, value: 0xE7E7E8E9, width: 4}
  - id: PeterLemon_SoundPCMMono
    rom: Sound/PCM/Mono/SoundPCMMono.gba
    cycles: 1000000
    skip_screenshot: true
    verify:
      memory:
      # FIFO reset bits read 0 on HW.
      - {address: 0x040000C6, value: 0xB640, width: 2}
      - {address: 0x04000082, value: 0x0304, width: 2}
      - {address: 0x04000084, value: 0x80, width: 1}
  - id: PeterLemon_SoundPCMStereo
    rom: Sound/PCM/Stereo/SoundPCMStereo.gba
    cycles: 1000000
    skip_screenshot: true
    verify:
      memory:
      # The bus reads 0x520C (FIFO reset bits read 0).
      - {address: 0x040000C6, value: 0xB640, width: 2}
      - {address: 0x040000D2, value: 0xB640, width: 2}
      - {address: 0x04000082, value: 0x520C, width: 2}
      - {address: 0x04000084, value: 0x80, width: 1}
  - id: PeterLemon_BIOSSoundBias
    rom: BIOS/Sound/Bias/BIOSSoundBias.gba
    cycles: 1000000
    skip_screenshot: true
    verify:
      memory:
      # TIMER var stores full TM0CNT; ROM checks low 16 bits only.
      - {address: 0x03000584, value: 0x47, width: 2}
  - id: PeterLemon_BIOSSoundChannelClear
    rom: BIOS/Sound/ChannelClear/BIOSSoundChannelClear.gba
    cycles: 1000000
    skip_screenshot: true
    verify:
      memory:
      - {address: 0x03000468, value: 0x52, width: 2}
  - id: PeterLemon_BIOSSoundDriverInit
    rom: BIOS/Sound/DriverInit/BIOSSoundDriverInit.gba
    cycles: 1000000
    skip_screenshot: true
  - id: PeterLemon_BIOSSoundDriverMain
    rom: BIOS/Sound/DriverMain/BIOSSoundDriverMain.gba
    cycles: 1000000
    skip_screenshot: true
  - id: PeterLemon_BIOSSoundDriverMode
    rom: BIOS/Sound/DriverMode/BIOSSoundDriverMode.gba
    cycles: 1000000
    skip_screenshot: true
  - id: PeterLemon_BIOSSoundDriverVSync
    rom: BIOS/Sound/DriverVSync/BIOSSoundDriverVSync.gba
    cycles: 1000000
    skip_screenshot: true
  - id: PeterLemon_BIOSSoundGetJumpList
    rom: BIOS/Sound/GetJumpList/BIOSSoundGetJumpList.gba
    cycles: 1000000
    skip_screenshot: true
  - id: PeterLemon_BIOSMidiKey2Freq
    rom: BIOS/Sound/MidiKey2Freq/BIOSMidiKey2Freq.gba
    cycles: 1000000
    skip_screenshot: true
  - id: PeterLemon_FastLine
    rom: FastLine/FastLine.gba
    cycles: 1000000
  - id: PeterLemon_FastLineClip
    rom: FastLineClip/FastLineClip.gba
    cycles: 1000000
  - id: PeterLemon_BIOSARCTAN
    rom: BIOS/Arithmetic/ARCTAN/BIOSARCTAN.gba
    cycles: 2000000
  - id: PeterLemon_BIOSDIV
    rom: BIOS/Arithmetic/DIV/BIOSDIV.gba
    cycles: 2000000
  # BIOSCHECKSUM pins B1 twice.
  - id: PeterLemon_BIOSCHECKSUM
    rom: BIOS/Misc/CHECKSUM/BIOSCHECKSUM.gba
    cycles: 2000000
  - id: PeterLemon_BIOSSQRT
    rom: BIOS/Arithmetic/SQRT/BIOSSQRT.gba
    cycles: 2000000
  - id: PeterLemon_BIOSBIT1BPP
    rom: BIOS/Decompress/BIT/1BPP/BIOSBIT1BPP.gba
    cycles: 3000000
  - id: PeterLemon_BIOSBIT2BPP
    rom: BIOS/Decompress/BIT/2BPP/BIOSBIT2BPP.gba
    cycles: 3000000
  - id: PeterLemon_BIOSBIT4BPP
    rom: BIOS/Decompress/BIT/4BPP/BIOSBIT4BPP.gba
    cycles: 3000000
  - id: PeterLemon_BIOSBIT8BPP
    rom: BIOS/Decompress/BIT/8BPP/BIOSBIT8BPP.gba
    cycles: 3000000
  - id: PeterLemon_BIOSHUFFMAN
    rom: BIOS/Decompress/HUFFMAN/BIOSHUFFMAN.gba
    cycles: 3000000
  - id: PeterLemon_BIOSLZ77
    rom: BIOS/Decompress/LZ77/BIOSLZ77.gba
    cycles: 3000000
  - id: PeterLemon_BIOSRegisterRamReset
    rom: BIOS/Reset/RegisterRam/BIOSRegisterRamReset.gba
    cycles: 3000000
  - id: PeterLemon_BIOSCPUSET
    rom: BIOS/MemoryCopy/CPUSET/BIOSCPUSET.gba
    cycles: 3000000
  - id: PeterLemon_BIOSCPUFASTSET
    rom: BIOS/MemoryCopy/CPUFASTSET/BIOSCPUFASTSET.gba
    cycles: 3000000
  - id: PeterLemon_BIOSRLE
    rom: BIOS/Decompress/RLE/BIOSRLE.gba
    cycles: 3000000
  - id: PeterLemon_BIOSDIFF
    rom: BIOS/Decompress/DIFF/BIOSDIFF.gba
    cycles: 3000000
  - id: PeterLemon_BIOSBGAFFINESET
    rom: BIOS/RotationScaling/BGAffineSet/BIOSBGAFFINESET.gba
    cycles: 3000000
  - id: PeterLemon_BIOSOBJAFFINESET
    rom: BIOS/RotationScaling/OBJAffineSet/BIOSOBJAFFINESET.gba
    cycles: 3000000
  - id: PeterLemon_Timers
    rom: Timers/Timers.gba
    cycles: 3000000
  - id: PeterLemon_BGMode0
    rom: LCD/BG/BGMode0/BGMode0.gba
    cycles: 1000000
  - id: PeterLemon_BGMode7
    rom: LCD/BG/BGMode7/BGMode7.gba
    cycles: 1000000
  - id: PeterLemon_BGRotZoomMode2
    rom: LCD/BG/BGRotZoomMode2/BGRotZoomMode2.gba
    cycles: 1000000
  - id: PeterLemon_BGRotZoomMode3
    rom: LCD/BG/BGRotZoomMode3/BGRotZoomMode3.gba
    cycles: 1000000
  - id: PeterLemon_BGRotZoomMode4
    rom: LCD/BG/BGRotZoomMode4/BGRotZoomMode4.gba
    cycles: 1000000
  - id: PeterLemon_BGRotZoomMode5
    rom: LCD/BG/BGRotZoomMode5/BGRotZoomMode5.gba
    cycles: 1000000
  - id: PeterLemon_OBJRotZoom4BPP
    rom: LCD/OBJ/OBJRotZoom4BPP/OBJRotZoom4BPP.gba
    cycles: 1000000
  - id: PeterLemon_OBJRotZoom8BPP
    rom: LCD/OBJ/OBJRotZoom8BPP/OBJRotZoom8BPP.gba
    cycles: 1000000
  - id: PeterLemon_GRB12LZDIFFRLEVideo
    rom: Video/GRB12LZDIFFRLEVideo/GRB12LZDIFFRLEVideo.gba
    cycles: 3000000
  - id: PeterLemon_Rickroll
    rom: "Video/Rickroll/Rick RPG Adventure [U].gba"
    cycles: 3000000
  - id: PeterLemon_GRB15LZVideo
    rom: Video/GRB15LZVideo/GRB15LZVideo.gba
    cycles: 3000000
  - id: PeterLemon_GRB12Decode120x80
    rom: Video/GRB12Decode120x80/GRB12Decode120x80.gba
    cycles: 3000000
  - id: PeterLemon_GRB12LZVideo
    rom: Video/GRB12LZVideo/GRB12LZVideo.gba
    cycles: 3000000
  - id: PeterLemon_GRB12Decode
    rom: Video/GRB12Decode/GRB12Decode.gba
    cycles: 3000000
  - id: PeterLemon_GRB12LZIPSVideo
    rom: Video/GRB12LZIPSVideo/GRB12LZIPSVideo.gba
    cycles: 15000000
  - id: PeterLemon_GRB15Decode
    rom: Video/GRB15Decode/GRB15Decode.gba
    cycles: 3000000
  - id: PeterLemon_DevoTimeOutForFun
    rom: Video/Devo-TimeOutForFun/Devo-TimeOutForFun.gba
    cycles: 3000000
  - id: PeterLemon_GRB15LZIPSVideo
    rom: Video/GRB15LZIPSVideo/GRB15LZIPSVideo.gba
    cycles: 3000000
  - id: PeterLemon_TouhouBadApple
    rom: "Video/Touhou-BadApple!/Touhou-BadApple!.gba"
    cycles: 3000000
  - id: PeterLemon_BigBuckBunny
    rom: Video/BigBuckBunny/BigBuckBunny.gba
    cycles: 3000000
  - id: PeterLemon_I4Decode
    rom: Video/I4Decode/I4Decode.gba
    cycles: 3000000
  - id: PeterLemon_3DEngine
    rom: 3DEngine/3DEngine.gba
    cycles: 1000000
  - id: PeterLemon_CylinderMap
    rom: CylinderMap/CylinderMap.gba
    cycles: 1000000
  - id: PeterLemon_Gravity
    rom: Physics/2D/Gravity/Gravity.gba
    cycles: 1000000
  - id: PeterLemon_Velocity
    rom: Physics/2D/Velocity/Velocity.gba
    # Interacted ref: 169xL + B + 22 flights; matches except ~90 edge pixels.
    cycles: 54430048
    inputs:
    - {cycle: 0, buttons: [l]}
    - {cycle: 47808544, buttons: []}
    - {cycle: 48089440, buttons: [b]}
    - {cycle: 48470336, buttons: []}
  - id: PeterLemon_Acceleration
    rom: Physics/2D/Acceleration/Acceleration.gba
    # Interacted ref: 48xLeft; matches except ~95 edge pixels.
    cycles: 14381920
    inputs:
    - {cycle: 0, buttons: [left]}
    - {cycle: 13820128, buttons: []}
- name: mgba-suite
  cases:
  # Menu-driven multi-test ROM: each case boots fresh (DOWN taps + A),
  # no buttons during the run; PASS:/FAIL: log lines branch per subtest.
  - id: mgba_suite_memory
    rom: suite.gba
    description: mgba suite - Memory tests
    cycles: 1000000000
    skip_screenshot: true
    script:
    - {press: [a], until_log: 'BEGIN: Memory tests'}
    - {press: [], until_log: 'END:'}
    verify:
      suite_log:
        begin: 'BEGIN: Memory tests'
        end: 'END:'
        pass_prefix: 'PASS: '
        fail_prefix: 'FAIL: '
        sram: {fail_suffix: ': FAIL', header_infix: ' test: ', detail_infix: ': Got'}
    # 1502/1552 pass. The 50 below are reference-side, not emulation.
    expected_checks:
    - 'ROM out-of-bounds load U8'
    - 'ROM out-of-bounds load S8'
    - 'ROM out-of-bounds load U16'
    - 'ROM out-of-bounds load U16 (unaligned)'
    - 'ROM out-of-bounds load S16'
    - 'ROM out-of-bounds load S16 (unaligned)'
    - 'ROM out-of-bounds load 32'
    - 'ROM out-of-bounds load 32 (unaligned 1)'
    - 'ROM out-of-bounds load 32 (unaligned 2)'
    - 'ROM out-of-bounds load 32 (unaligned 3)'
    - 'ROM out-of-bounds load DMA1 16'
    - 'ROM out-of-bounds load DMA1 16 (unaligned)'
    - 'ROM out-of-bounds load DMA1 32'
    - 'ROM out-of-bounds load DMA1 32 (unaligned 1)'
    - 'ROM out-of-bounds load DMA1 32 (unaligned 2)'
    - 'ROM out-of-bounds load DMA1 32 (unaligned 3)'
    - 'ROM out-of-bounds load DMA2 16'
    - 'ROM out-of-bounds load DMA2 16 (unaligned)'
    - 'ROM out-of-bounds load DMA2 32'
    - 'ROM out-of-bounds load DMA2 32 (unaligned 1)'
    - 'ROM out-of-bounds load DMA2 32 (unaligned 2)'
    - 'ROM out-of-bounds load DMA2 32 (unaligned 3)'
    - 'ROM out-of-bounds load DMA3 16'
    - 'ROM out-of-bounds load DMA3 16 (unaligned)'
    - 'ROM out-of-bounds load DMA3 32'
    - 'ROM out-of-bounds load DMA3 32 (unaligned 1)'
    - 'ROM out-of-bounds load DMA3 32 (unaligned 2)'
    - 'ROM out-of-bounds load DMA3 32 (unaligned 3)'
    - 'ROM out-of-bounds load swi B 16'
    - 'ROM out-of-bounds load swi B 16 (unaligned)'
    - 'ROM out-of-bounds load swi B 32'
    - 'ROM out-of-bounds load swi B 32 (unaligned 1)'
    - 'ROM out-of-bounds load swi B 32 (unaligned 2)'
    - 'ROM out-of-bounds load swi B 32 (unaligned 3)'
    - 'ROM out-of-bounds load swi C 32'
    - 'ROM out-of-bounds load swi C 32 (unaligned 1)'
    - 'ROM out-of-bounds load swi C 32 (unaligned 2)'
    - 'ROM out-of-bounds load swi C 32 (unaligned 3)'
    - 'BIOS load U8'
    - 'BIOS load S8'
    - 'BIOS load U16'
    - 'BIOS load U16 (unaligned)'
    - 'BIOS load S16'
    - 'BIOS load S16 (unaligned)'
    - 'BIOS load 32'
    - 'BIOS load 32 (unaligned 1)'
    - 'BIOS load 32 (unaligned 2)'
    - 'BIOS load 32 (unaligned 3)'
    - 'BIOS out-of-bounds load S16 (unaligned)'
    - 'Out-of-bounds load S16 (unaligned)'
  # Remaining groups are menu-driven (DOWN x N, A to run). Each case
  # boots fresh, so the menu always starts at index 0.
  - id: mgba_suite_io_read
    rom: suite.gba
    description: mgba suite - I/O read tests
    cycles: 1000000000
    skip_screenshot: true
    script:
    - {press: [], wait_frames: 30}
    - {press: [down], wait_frames: 3}
    - {press: [a], until_log: 'BEGIN: I/O read tests'}
    - {press: [], until_log: 'END:'}
    verify:
      suite_log:
        begin: 'BEGIN: I/O read tests'
        end: 'END:'
        pass_prefix: 'PASS: '
        fail_prefix: 'FAIL: '
    # All subtests pass. Unreadable APU bits never persist (write-time
    # masks); SOUNDCNT_X reads 0x0080.
  - id: mgba_suite_shifter
    rom: suite.gba
    description: mgba suite - Shifter tests
    cycles: 1000000000
    skip_screenshot: true
    script:
    - {press: [], wait_frames: 30}
    - {press: [down], wait_frames: 3}
    - {press: [down], wait_frames: 3}
    - {press: [down], wait_frames: 3}
    - {press: [down], wait_frames: 3}
    - {press: [down], wait_frames: 3}
    - {press: [a], until_log: 'BEGIN: Shifter tests'}
    - {press: [], until_log: 'END:'}
    verify:
      suite_log:
        begin: 'BEGIN: Shifter tests'
        end: 'END:'
        pass_prefix: 'PASS: '
        fail_prefix: 'FAIL: '
  - id: mgba_suite_carry
    rom: suite.gba
    description: mgba suite - Carry tests
    cycles: 1000000000
    skip_screenshot: true
    script:
    - {press: [], wait_frames: 30}
    - {press: [down], wait_frames: 3}
    - {press: [down], wait_frames: 3}
    - {press: [down], wait_frames: 3}
    - {press: [down], wait_frames: 3}
    - {press: [down], wait_frames: 3}
    - {press: [down], wait_frames: 3}
    - {press: [a], until_log: 'BEGIN: Carry tests'}
    - {press: [], until_log: 'END:'}
    verify:
      suite_log:
        begin: 'BEGIN: Carry tests'
        end: 'END:'
        pass_prefix: 'PASS: '
        fail_prefix: 'FAIL: '
  - id: mgba_suite_multiply_long
    rom: suite.gba
    description: mgba suite - Multiply long tests
    cycles: 1000000000
    skip_screenshot: true
    script:
    - {press: [], wait_frames: 30}
    - {press: [down], wait_frames: 3}
    - {press: [down], wait_frames: 3}
    - {press: [down], wait_frames: 3}
    - {press: [down], wait_frames: 3}
    - {press: [down], wait_frames: 3}
    - {press: [down], wait_frames: 3}
    - {press: [down], wait_frames: 3}
    - {press: [a], until_log: 'BEGIN: Multiply long tests'}
    - {press: [], until_log: 'END:'}
    verify:
      suite_log:
        begin: 'BEGIN: Multiply long tests'
        end: 'END:'
        pass_prefix: 'PASS: '
        fail_prefix: 'FAIL: '
    # All 72 pass. MULLS C is the multiplier's final carry, not the product.
  - id: mgba_suite_bios_math
    rom: suite.gba
    description: mgba suite - BIOS math tests
    cycles: 1000000000
    skip_screenshot: true
    script:
    - {press: [], wait_frames: 30}
    - {press: [down], wait_frames: 3}
    - {press: [down], wait_frames: 3}
    - {press: [down], wait_frames: 3}
    - {press: [down], wait_frames: 3}
    - {press: [down], wait_frames: 3}
    - {press: [down], wait_frames: 3}
    - {press: [down], wait_frames: 3}
    - {press: [down], wait_frames: 3}
    - {press: [a], until_log: 'BEGIN: BIOS math tests'}
    - {press: [], until_log: 'END:'}
    verify:
      suite_log:
        begin: 'BEGIN: BIOS math tests'
        end: 'END:'
        pass_prefix: 'PASS: '
        fail_prefix: 'FAIL: '
  - id: mgba_suite_sio_read
    rom: suite.gba
    description: mgba suite - SIO register R/W tests
    cycles: 1000000000
    skip_screenshot: true
    script:
    - {press: [], wait_frames: 30}
    - {press: [down], wait_frames: 3}
    - {press: [down], wait_frames: 3}
    - {press: [down], wait_frames: 3}
    - {press: [down], wait_frames: 3}
    - {press: [down], wait_frames: 3}
    - {press: [down], wait_frames: 3}
    - {press: [down], wait_frames: 3}
    - {press: [down], wait_frames: 3}
    - {press: [down], wait_frames: 3}
    - {press: [down], wait_frames: 3}
    - {press: [a], until_log: 'BEGIN: SIO register R/W tests'}
    - {press: [], until_log: 'END:'}
    verify:
      suite_log:
        begin: 'BEGIN: SIO register R/W tests'
        end: 'END:'
        pass_prefix: 'PASS: '
        fail_prefix: 'FAIL: '
    # 90/90 GRADUATED. Unconnected multi is a slave (Multi tests self-SKIP).
  - id: mgba_suite_misc_edge
    rom: suite.gba
    description: mgba suite - Misc. edge case tests
    cycles: 1000000000
    skip_screenshot: true
    script:
    - {press: [], wait_frames: 30}
    - {press: [down], wait_frames: 3}
    - {press: [down], wait_frames: 3}
    - {press: [down], wait_frames: 3}
    - {press: [down], wait_frames: 3}
    - {press: [down], wait_frames: 3}
    - {press: [down], wait_frames: 3}
    - {press: [down], wait_frames: 3}
    - {press: [down], wait_frames: 3}
    - {press: [down], wait_frames: 3}
    - {press: [down], wait_frames: 3}
    - {press: [down], wait_frames: 3}
    - {press: [down], wait_frames: 3}
    - {press: [a], until_log: 'BEGIN: Misc. edge case tests'}
    - {press: [], until_log: 'END:'}
    verify:
      suite_log:
        begin: 'BEGIN: Misc. edge case tests'
        end: 'END:'
        pass_prefix: 'PASS: '
        fail_prefix: 'FAIL: '
        sram: {fail_suffix: ': FAIL', header_infix: ' test: ', detail_infix: ': Got'}
    # 6 fail on research-grade micro-behavior; boxed.
    expected_checks:
    - 'DMA Prefetch Break'
    - 'DMA Prefetch Read'
    - 'H-blank bit start Hblank'
    - 'H-blank bit start Flip 1'
    - 'H-blank bit start Flip 2'
    - 'H-blank bit start Flip 3'
  # Batch 2: timing-sensitive suites (slower guests; larger budgets).
  - id: mgba_suite_timing
    rom: suite.gba
    description: mgba suite - Timing tests
    cycles: 8000000000
    skip_screenshot: true
    script:
    - {press: [], wait_frames: 30}
    - {press: [down], wait_frames: 3}
    - {press: [down], wait_frames: 3}
    - {press: [a], until_log: 'BEGIN: Timing tests'}
    - {press: [], until_log: 'END:'}
    verify:
      suite_log:
        begin: 'BEGIN: Timing tests'
        end: 'END:'
        pass_prefix: 'PASS: '
        fail_prefix: 'FAIL: '
        sram: {fail_suffix: ': FAIL', header_infix: ' test: ', detail_infix: ': Got'}
  - id: mgba_suite_timers
    rom: suite.gba
    description: mgba suite - Timer count-up tests
    # Full suite needs ~61M tcycles to reach END; 100M keeps headroom.
    cycles: 100000000
    skip_screenshot: true
    script:
    - {press: [], wait_frames: 30}
    - {press: [down], wait_frames: 3}
    - {press: [down], wait_frames: 3}
    - {press: [down], wait_frames: 3}
    - {press: [a], until_log: 'BEGIN: Timer count-up tests'}
    - {press: [], until_log: 'END:'}
    verify:
      suite_log:
        begin: 'BEGIN: Timer count-up tests'
        end: 'END:'
        pass_prefix: 'PASS: '
        fail_prefix: 'FAIL: '
        sram: {fail_suffix: ': FAIL', header_infix: ' test: ', detail_infix: ': Got'}
  - id: mgba_suite_timer_irq
    rom: suite.gba
    description: mgba suite - Timer IRQ tests
    cycles: 8000000000
    skip_screenshot: true
    script:
    - {press: [], wait_frames: 30}
    - {press: [down], wait_frames: 3}
    - {press: [down], wait_frames: 3}
    - {press: [down], wait_frames: 3}
    - {press: [down], wait_frames: 3}
    - {press: [a], until_log: 'BEGIN: Timer IRQ tests'}
    - {press: [], until_log: 'END:'}
    verify:
      suite_log:
        begin: 'BEGIN: Timer IRQ tests'
        end: 'END:'
        pass_prefix: 'PASS: '
        fail_prefix: 'FAIL: '
  - id: mgba_suite_dma
    rom: suite.gba
    description: mgba suite - DMA tests
    cycles: 8000000000
    skip_screenshot: true
    script:
    - {press: [], wait_frames: 30}
    - {press: [down], wait_frames: 3}
    - {press: [down], wait_frames: 3}
    - {press: [down], wait_frames: 3}
    - {press: [down], wait_frames: 3}
    - {press: [down], wait_frames: 3}
    - {press: [down], wait_frames: 3}
    - {press: [down], wait_frames: 3}
    - {press: [down], wait_frames: 3}
    - {press: [down], wait_frames: 3}
    - {press: [a], until_log: 'BEGIN: DMA tests'}
    - {press: [], until_log: 'END:'}
    verify:
      suite_log:
        begin: 'BEGIN: DMA tests'
        end: 'END:'
        pass_prefix: 'PASS: '
        fail_prefix: 'FAIL: '
    # All pass (END: 1244/1244). ROM sources increment; +2 shift needs primed bursts.
  - id: mgba_suite_sio_timing
    rom: suite.gba
    description: mgba suite - SIO timing tests
    cycles: 8000000000
    skip_screenshot: true
    script:
    - {press: [], wait_frames: 30}
    - {press: [down], wait_frames: 3}
    - {press: [down], wait_frames: 3}
    - {press: [down], wait_frames: 3}
    - {press: [down], wait_frames: 3}
    - {press: [down], wait_frames: 3}
    - {press: [down], wait_frames: 3}
    - {press: [down], wait_frames: 3}
    - {press: [down], wait_frames: 3}
    - {press: [down], wait_frames: 3}
    - {press: [down], wait_frames: 3}
    - {press: [down], wait_frames: 3}
    - {press: [a], until_log: 'BEGIN: SIO timing tests'}
    - {press: [], until_log: 'END:'}
    verify:
      suite_log:
        begin: 'BEGIN: SIO timing tests'
        end: 'END:'
        pass_prefix: 'PASS: '
        fail_prefix: 'FAIL: '
    # All 8 GRADUATED. 4 Normal = transfer-cycles + 121; 4 Multi self-SKIP.
